#include "holberton.h"

/**
 * main - This function computes the absolute value of an integer
 *
 * Return: absolut value
 */
int _abs(int a)
{
	if (a > 0)
	{
		return (a);
	}
	else
	{
		return (-a);
	}
}
