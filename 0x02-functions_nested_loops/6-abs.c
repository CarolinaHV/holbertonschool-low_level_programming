#include "holberton.h"

/**
 * _abs - This function computes the absolute value of an integer
 * @a: integer number
 * Return: absolut value
 */
int _abs(int a)
{
	if (a > 0)
	{
		return (a);
	}
	else
	{
		return (-a);
	}
}
