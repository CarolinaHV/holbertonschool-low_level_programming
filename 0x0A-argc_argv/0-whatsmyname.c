#include <stdio.h>
/**
 * main - This program prints its name,
 * followed by a new line.
 * @argc: integer count
 * @argv: array vector
 * Return: value 0
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
